fastapi>=0.120.3
uvicorn[standard]>=0.32.1
sqlalchemy[asyncio]>=2.0.36
alembic>=1.17.1
asyncpg==0.30.0
psycopg2-binary>=2.9.11
pydantic[email]>=2.10.3
pydantic-settings>=2.6.1
email-validator>=2.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt MUST stay below 4.1. passlib 1.7.4 reads bcrypt.__about__.__version__,
# removed in bcrypt 4.1, and bcrypt 5.x stopped silently truncating at 72 bytes.
# With an unpinned bcrypt a fresh install resolves to 5.x and CryptContext.hash()
# raises ValueError — password authentication is broken outright, not degraded.
# Verified: bcrypt 5.0.0 fails, bcrypt 4.0.1 hashes and verifies correctly.
bcrypt==4.0.1
cryptography>=43.0.0
python-multipart>=0.0.20
redis==5.2.1
sentence-transformers>=3.0.0
numpy>=1.26.0
celery==5.4.0
anthropic==0.40.0
openai>=1.12.0
boto3==1.35.92
pgvector>=0.4.1
httpx>=0.28.1
aiofiles>=24.1.0
python-magic>=0.4.27
pytest>=8.3.4
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
slowapi>=0.1.9
# Vectorize.io Hindsight — optional enhanced memory (HINDSIGHT_ENABLED=true to activate)
hindsight-all

# Sarvam voice integration
httpx[http2]>=0.27
websockets>=12.0
PyJWT[crypto]>=2.8
