# Build database URL from environment variables
POSTGRES_HOST = os.getenv("POSTGRES_HOST", "db")
POSTGRES_PORT = os.getenv("POSTGRES_PORT", "5432")
POSTGRES_USER = os.getenv("POSTGRES_USER", "pal")
POSTGRES_PASSWORD = os.getenv("POSTGRES_PASSWORD", "change_me_in_prod")
POSTGRES_DB = os.getenv("POSTGRES_DB", "pal")

DATABASE_URL = f"postgresql://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{POSTGRES_HOST}:{POSTGRES_PORT}/{POSTGRES_DB}"
WEBHOOK_SECRET = os.getenv("WEBHOOK_SECRET", "")  # Optional signature verification
